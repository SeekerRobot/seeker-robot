# extra_packages

Includes all the extra ros packages needed for MicroROS. These are normally in the forms of msg/srv/action-only packages.

Inside the docker container, mcu_msgs will be mounted inside of extra_packages to be included in the MicroROS build.
